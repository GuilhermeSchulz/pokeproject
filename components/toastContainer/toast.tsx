import { ToastContainer as ReactToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";

const ToastContainer: React.FC = () => <ReactToastContainer />;

export default ToastContainer;
